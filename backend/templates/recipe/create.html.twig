{% extends "base.html.twig" %}

{% block body %}
    {{form_start(form)}}
    {{form_widget(form)}}
    <input type="submit" value="Subir receta" class="btn btn-success">
    {{form_end(form)}}
    <a href="recipes" class="btn btn-danger">Volver</a>
{% endblock %}