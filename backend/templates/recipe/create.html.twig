{% extends "base.html.twig" %}

{% block body %}
    {{form_start(form)}}
    {{form_widget(form)}}
    <input type="submit" value="Subir receta" class="btn btn-success">
    {{form_end(form)}}
    <a href="{{path('recipes')}}" class="btn btn-success">Volver</a>
{% endblock %}